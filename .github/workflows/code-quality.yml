name: Code Quality

on:
  pull_request:
    branches: [ master, develop ]
  push:
    branches: [ master, develop ]

jobs:
  code-quality:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Code formatting check
      run: |
        black --check --diff . || echo "Code formatting issues found, but continuing..."

    - name: Import sorting check
      run: |
        isort --check-only --diff . || echo "Import sorting issues found, but continuing..."

    - name: Linting
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics || echo "Critical linting errors found"
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || echo "Linting warnings found, but continuing..."

    - name: Type checking
      run: |
        mypy . --ignore-missing-imports || echo "Type checking issues found, but continuing..."

    - name: Security check
      run: |
        bandit -r . -f json -o bandit-report.json || echo "Security issues found, but continuing..."
        safety check --file requirements.txt || echo "Dependency vulnerabilities found, but continuing..."

    - name: Upload security report
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: security-report
        path: bandit-report.json
